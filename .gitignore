.env
node_modules/
dist/

# User-added worlds (not bundled) — keep large files out of git for custom setups
# To exclude a specific world: public/worlds/my-world/splat.spz

# Character GLBs — committed; thumbnails generated locally
# (uncomment below to exclude large character files from git)
# public/characters/*/character.glb

.DS_Store
*.local
.vercel
